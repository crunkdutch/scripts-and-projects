/** * PointRoll Base Progress *  * Holds a list of basic progress bar functions *  * @class 		PRVCUIProgress * @package		PointRollAPI.components.video.controls * @author		PointRoll */  package PointRollAPI_AS3.components.video.controls  {	import flash.events.TimerEvent;	import flash.utils.Timer;	/** @private **/
	public class PRVCUIProgress extends PointRollAPI_AS3.components.video.controls.PRVCBase {				protected var maxWidth:Number = 0;				protected var timerNotify:Timer;			//Delayed init, to wait until the VDC creates its handlers				function PRVCUIProgress() {			super();		}				protected override function init():void {			super.init();			timerNotify = new Timer(300);			timerNotify.addEventListener("timer", initialize);              timerNotify.start();  		}				protected function initialize(event:TimerEvent):void {			//		}				protected function addListeners():void {}				/**		*	Resets the progress		*/		public function reset():void {			showProgress(1);		}				/**		*	Shows the progress, receives a number between 0-100		*/		protected function showProgress(progress:Number):void {			//		}						/**		*	Sets the width of the progress bar		*/		protected function setSize(width:Number):void {			//		}					/**		*	Hides the track bar		*/		protected function hideTrack():void {			//		}			}		 }